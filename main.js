// main.js
window.addEventListener('DOMContentLoaded', function () {
  // Bind core logic on page load
  form_bindEvents();
  verdict_bindEvents();
  photo_bindEvents();
  pdf_bindExportButton();
});
